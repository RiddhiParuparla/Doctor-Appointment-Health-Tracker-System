{
  "name": "healthtracker-monorepo",
  "private": true,
  "scripts": {
    "install-client": "cd client && npm install",
    "build-client": "cd client && npm run build",
    "build": "npm run install-client && npm run build-client"
  }
}
